# Copy this file to .env and adjust if needed. Both docker-compose.yml and the
# backend app read these same variable names — docker-compose auto-loads a
# root-level .env; the backend needs them exported into its shell/process env.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=goal_tracker
DB_USER=goal_tracker
DB_PASSWORD=change-me
